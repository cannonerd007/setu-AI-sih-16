// Stitch DESIGN.md touch-target mandate.
export const touchTarget = {
  min: 48, // every actionable area, dp
  primaryCta: 64, // Push-to-Talk / primary record button, dp
} as const

export const micButton = {
  core: 96,
  ring: 128,
} as const
